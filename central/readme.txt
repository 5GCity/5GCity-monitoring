scompattare .zip
installare exeportes sul nodo locale e sui nodi target con la seguente procedura
se non esiste creare frontend/database
se non esiste /prometheus/prometheus.yml copiare /prometheus/prometheus.yml.base in /prometheus/prometheus.yml
lanciare docker-compose.yml -d up

Variabile d'ambiente per recuperare l'indirizzo di management.

